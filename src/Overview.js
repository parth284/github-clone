import React from 'react';
import "./Overview.css"

const Overview = ({followersList, followingsList, starres, repos}) => {
    return (
        <div className="overview">
            overview
        </div>
    );
}

export default Overview;